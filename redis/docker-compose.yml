version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: katago-redis
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD:-blackrice123}
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: always

volumes:
  redis_data:
