apiVersion: v1
kind: Service
metadata:
  name: katago-service
spec:
  selector:
    app: katago-server
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8080
  sessionAffinity: ClientIP
  type: LoadBalancer
